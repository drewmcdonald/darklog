# Claude Notes

## Project Setup

**This is a Bun project** - Use `bun` commands instead of `npm`:
- `bun install` instead of `npm install`
- `bun run build` instead of `npm run build`
- `bun run dev` instead of `npm run dev`
- etc.
